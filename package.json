{
  "name": "sw-healing",
  "version": "1.0.0",
  "description": "수원 힐링 상담센터",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && wrangler dev --env development",
    "dev:frontend": "cd frontend && npm run dev",
    "start": "npm run dev"
  },
  "dependencies": {
    "@libsql/client": "^0.15.15"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
